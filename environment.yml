# Article 4 — Conda environment specification
# Reproduce with:  conda env create -f environment.yml

name: tese-h2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - -r requirements.txt
